//This needs the values filling in, and saving as secrets.ts in the same directory
export const elasticSearchToken = `ApiKey {someApiKey}`
export const openAiToken = 'The api key without a prefix'
export const azureAiToken = 'A jwt token that is valid for the user.'
export const azureAiUrl = 'https://{the url}/proxy'